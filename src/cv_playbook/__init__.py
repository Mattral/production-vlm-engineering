"""cv_playbook: shared utilities for the Frontier CV Engineering Playbook.

Modules:
    config        Pydantic v2 schemas for experiment configs.
    drift         Embedding drift detection (DINO/CLIP/VLM encoder space).
    eval          Grounding, faithfulness, and numeric-accuracy metrics for chart/doc VQA.
    utils         Synthetic chart data generation, seeding, logging helpers.
"""

__version__ = "0.1.0"
